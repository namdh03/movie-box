import { Box, styled } from "@mui/material";
import theme from "@themes/index";

export const SuggestSection = styled(Box)`
    height: 100vh;
    padding: 160px 0 124px;
    background: ${theme.palette.background.main};
`;
