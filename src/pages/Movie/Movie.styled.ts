import { Box, styled } from "@mui/material";
import theme from "@themes/index";

export const MovieBannerSection = styled(Box)`
    padding: 144px 0 156px;
    background: ${theme.palette.background.main};
`;
